<script is:inline>
  window.handleAiCommand = function (text, format) {
    // Temporary implementation
    console.log("AI Assistant received:", text);
    console.log("With format:", format);
    window.showSuccess(`Added ${format} formatted text`, 2000);
  };
</script>
